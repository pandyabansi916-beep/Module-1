
1)create db with the name of tops
-> create database tops;
2)create table with the name of students and predefined column is
id and student_name
->CREATE TABLE STUDENT(
    ID INT AUTO_INCREMENT PRIMARY KEY,
    STUDENT_NAME VARCHAR(50)
);    
3)After creation of table add one more column student_fees
->ALTER TABLE STUDENT
  ADD COLUMN student_FEES INT;
4)After creation of table and student_fees add one more column student_grdae
->ALTER TABLE STUDENT
  ADD COLUMN student_GRADE VARCHAR(50);
5)change datatype of student grade
->ALTER TABLE STUDENT
  Modify student_grade varchar(10);
6)remove column student_City
->ALTER TABLE STUDENT
 DROP COLUMN student_city;
7)Insert data in all columns
->insert into student(student_name,student_fees,student_grade)
  values('Bansi',25000,'A+');
  insert into student(student_name,student_fees,student_grade)
  values('Nikunj',25000,'B+');
  insert into student(student_name,student_fees,student_grade)
  Values('siddhi',25000,'A+');
8)update data in any one column
->update student
  set student_name='Darshil' where id=3;
9)delete data from any column
->delete from student where student=2;
10)select data according to grades
->select student_grade
  from students;
11)select data according to column wise
->select student_name
  from students;

